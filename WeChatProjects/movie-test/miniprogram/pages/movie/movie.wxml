<!--miniprogram/pages/movie/movie.wxml-->
<view class="movie" wx:for="{{movieList}}" wx:key="{{index}}">
  <view class="movieinfo">{{item.text}}</view>
  <image src="{{item.header}}"></image>
  <view class="name">作者：{{item.name}}</view>
  <view>上传时间：{{item.passtime}}</view>
</view>
